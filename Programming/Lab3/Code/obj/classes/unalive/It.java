package obj.classes.unalive;

import obj.classes.Entity;
import obj.enums.ActionEnum;

public class It extends Entity {
    public It(String name, ActionEnum actionEnum) {
        super(name, actionEnum);
    }
}
